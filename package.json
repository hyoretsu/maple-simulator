{
	"name": "maple-simulator",
	"private": true,
	"workspaces": [
		"backend",
		"frontend"
	],
	"scripts": {
		"prepare": "husky install"
	},
	"lint-staged": {
		"backend/src/**/*.ts": [
			"rome check --apply",
			"npm run -w backend test --bail"
		],
		"frontend/src/**/*.(ts|tsx)": [
			"rome check --apply",
			"npm run -w frontend test --bail"
		]
	},
	"devDependencies": {
		"husky": "^8.0.3",
		"lint-staged": "^13.2.1",
		"rome": "^12.0.0",
		"typescript": "^5.0.4"
	}
}
