{
	"name": "maple-simulator",
	"private": true,
	"scripts": {
		"prepare": "husky install"
	},
	"lint-staged": {
		"backend/src/**/*.ts": [
			"rome check --apply",
			"pnpm -F backend test --bail"
		],
		"frontend/src/**/*.(ts|tsx)": [
			"rome check --apply",
			"pnpm -F frontend test --bail"
		]
	},
	"devDependencies": {
		"husky": "^8.0.3",
		"lint-staged": "^13.2.1",
		"rome": "^12.0.0",
		"typescript": "^5.0.4"
	}
}
